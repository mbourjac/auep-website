import { Heading } from './heading';

export const Main = () => {
  return (
    <main className="grid grow grid-cols-[auto_1fr] border border-blue-500 bg-blue-500">
      <Heading />
    </main>
  );
};
