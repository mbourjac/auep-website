export type ContactPerson = {
  name: string;
  institutions: string[];
  email: string;
};

export type Institution = {
  name: string;
  address: string;
  city: string;
  picture: string;
  link: string;
};
